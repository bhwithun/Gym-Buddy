# Pooled Neon connection string for the gymbuddy database.
DATABASE_URL=

# Optional. When set, every path except GET /, GET /routine, and GET /health
# requires Authorization: Bearer <token>. Leave empty to match an open worker.
INGEST_TOKEN=
